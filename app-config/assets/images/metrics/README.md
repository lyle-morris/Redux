# Metric Images

Upload any metric-specific app-config artwork here. Use lowercase descriptive
filenames with hyphens, such as `calendar.png`, `weather.png`, `steps.png`, and
`battery.png`.

Keep transparent artwork on a consistent canvas size. The app-config CSS will
control its displayed dimensions.
